applications:
  - name: chuck-repo
    path: build/libs/chuck-repo.jar
    buildpack: java_buildpack
    random-route: true
  - name: chuck-service
    path: build/libs/chuck-service.jar
    buildpack: java_buildpack
    random-route: true
    services:
      - chuck-dashboard
